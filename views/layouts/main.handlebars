<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/style.css" />
    <title>Renterly</title>
</head>

{{!-- The menu including Home, Dashboard, and Login/Logout --}}

<body class="bg-light">
    <header class="">
        <nav class="navbar navbar-light bg-red px-3 nav-renterly d-flex flex-row">
            <img class="logo" src="images/logo.jpg"alt="">
            <a class="navbar-brand w-50 text-left" href="/">
                <h1 class="d-inline w-100 text-left" style="color:white">Renterly </h1>
            </a>

            <div class="d-flex flex-row">
                <a class="btn btn-secondary" style="margin-right: 10px" href="/">Home</a>

                <!-- render login or logout links if user is logged in or out-->
                {{#if logged_in}}
                <a class="btn btn-info" style="margin-right: 8px" href="/dashboard">Dashboard</a>
                <button class="btn btn-dark" id="logout">Logout</button>
                {{else}}
                <a class="btn btn-primary" href="/login">Login</a>
                {{/if}}
            </div>
        </nav>
    </header>
    <main class="container container-fluid mt-5">
        <!-- Render the sub handlebar layouts -->
        {{{ body }}}
    </main>

    <footer>
    </footer>


    <!-- Logout script for logged in users only -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>